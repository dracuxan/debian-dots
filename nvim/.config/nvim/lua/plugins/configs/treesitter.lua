local options = {
  ensure_installed = { "lua", "vim", "vimdoc", "java", "go", "elixir", "eex", "heex" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
